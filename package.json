{
  "name": "mgcc",
  "version": "0.0.1",
  "private": true,
  "description": "Machine Gun Command Center - operating platform for Machine Gun Spray Foam & Concrete Lifting LLC.",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build:gs": "node tools/deploy/build_gs_bundle.js",
    "deploy": "pnpm build:gs && cd app && clasp push && clasp deploy",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
